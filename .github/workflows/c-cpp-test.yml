name: Windows MSVC Build with vcpkg cache

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Cache vcpkg installed libraries
        uses: actions/cache@v3
        with:
          path: |
            C:\Dev\vcpkg\installed
            C:\Dev\vcpkg\packages
          key: vcpkg-${{ runner.os }}-${{ hashFiles('**/vcpkg.json') }}
          restore-keys: |
            vcpkg-${{ runner.os }}-

      - name: Clone vcpkg to C:\Dev\vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git C:\Dev\vcpkg
          C:\Dev\vcpkg\bootstrap-vcpkg.bat

      - name: Install libraries with vcpkg (skip if cached)
        run: |
          C:\Dev\vcpkg\vcpkg.exe install

      - name: Setup MSBuild manually for Community Edition
        run: |
          $msbuildPath = "C:\Dev\IDE\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin"
          $env:PATH = "$msbuildPath;$env:PATH"
          & "$msbuildPath\msbuild.exe" -version
        shell: pwsh

      - name: Build all .sln files with vcpkg + MySQL include
        shell: cmd
        run: |
          set "mysqlInclude=C:\Program Files\mysql-connector-c++-9.2.0-winx64\include;C:\Program Files\mysql-connector-c++-9.2.0-winx64\include\mysql"
          set "mysqlLib=C:\Program Files\mysql-connector-c++-9.2.0-winx64\lib64"
          set "msbuildPath=C:\Dev\IDE\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe"
          for /r %%F in (*.sln) do (
            echo Building %%F
            "%msbuildPath%" "%%F" ^
              /p:Configuration=Debug ^
              /p:Platform=x64 ^
              /p:VcpkgRoot=C:\Dev\vcpkg ^
              /p:VcpkgTriplet=x64-windows ^
              /p:VcpkgEnableManifest=true ^
              /p:AdditionalIncludeDirectories=%mysqlInclude% ^
              /p:AdditionalLibraryDirectories=%mysqlLib%
          )

      - name: Lint HTML, CSS, JS
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install linters
        run: |
          npm install -g eslint stylelint htmlhint

      - name: Run ESLint (JavaScript)
        run: eslint ./path/to/js/files --ext .js

      - name: Run Stylelint (CSS)
        run: stylelint "./path/to/css/**/*.css"

      - name: Run HTMLHint
        run: htmlhint ./path/to/html/files


  # docker-build:
  #   needs: lint-and-test
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Log in to DockerHub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}

  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: yourdockerhub/repo:latest

