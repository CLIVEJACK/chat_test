name: Windows MSVC Build with vcpkg

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Clone vcpkg to C:\Dev\vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git C:\Dev\vcpkg
          C:\Dev\vcpkg\bootstrap-vcpkg.bat

      - name: Install required libraries via vcpkg
        run: |
          C:\Dev\vcpkg\vcpkg.exe install cpp-httplib redis-plus-plus mysql-connector-cpp:x64-windows

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.3.1

      - name: Build all .sln files with vcpkg integration
        run: |
          Get-ChildItem -Recurse -Filter *.sln | ForEach-Object {
            Write-Host "Building $($_.FullName)"
            msbuild $_.FullName `
              /p:Configuration=Debug `
              /p:Platform=x64 `
              /p:VcpkgRoot=C:\Dev\vcpkg `
              /p:VcpkgTriplet=x64-windows
          }
        shell: pwsh


      - name: Lint HTML, CSS, JS
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install linters
        run: |
          npm install -g eslint stylelint htmlhint

      - name: Run ESLint (JavaScript)
        run: eslint ./path/to/js/files --ext .js

      - name: Run Stylelint (CSS)
        run: stylelint "./path/to/css/**/*.css"

      - name: Run HTMLHint
        run: htmlhint ./path/to/html/files

  # docker-build:
  #   needs: lint-and-test
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Log in to DockerHub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}

  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: yourdockerhub/repo:latest

